bazeldnf = use_extension("@bazeldnf//bazeldnf:extensions.bzl", "bazeldnf")

bazeldnf.config(
    name = "bazeldnf-centos-aarch64",
    lock_file = "//rpm/lock-files:centos-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["acl", "curl-minimal", "vim-minimal", "coreutils-single", "glibc-minimal-langpack", "libcurl-minimal"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-testimage-aarch64",
    lock_file = "//rpm/lock-files:testimage-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["device-mapper", "e2fsprogs", "iputils", "nmap-ncat", "procps-ng", "qemu-img", "sevctl", "tar", "targetcli", "util-linux", "which"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-libvirt-aarch64",
    lock_file = "//rpm/lock-files:libvirt-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["libvirt-devel", "keyutils-libs", "krb5-libs", "libmount", "lz4-libs"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-sandboxroot-aarch64",
    lock_file = "//rpm/lock-files:sandboxroot-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["findutils", "gcc", "glibc-static", "python3", "sssd-client"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-launcherbase-aarch64",
    lock_file = "//rpm/lock-files:launcherbase-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["libvirt-client", "libvirt-daemon-driver-qemu", "passt", "qemu-kvm-core", "qemu-kvm-device-usb-host", "swtpm-tools", "findutils", "nftables", "nmap-ncat", "procps-ng", "selinux-policy", "selinux-policy-targeted", "tar", "virtiofsd", "xorriso", "edk2-aarch64", "qemu-kvm-device-usb-redirect", "qemu-kvm-device-display-virtio-gpu", "qemu-kvm-device-display-virtio-gpu-pci"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-handlerbase-aarch64",
    lock_file = "//rpm/lock-files:handlerbase-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["qemu-img", "findutils", "iproute", "nftables", "procps-ng", "selinux-policy", "selinux-policy-targeted", "tar", "util-linux", "xorriso"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-libguestfstools-aarch64",
    lock_file = "//rpm/lock-files:libguestfstools-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["libguestfs", "guestfs-tools", "libvirt-daemon-driver-qemu", "qemu-kvm-core", "selinux-policy", "selinux-policy-targeted"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = ["^(kernel-|linux-firmware)", "^(python[3]{0,1}-)", "^mozjs60", "^(libvirt-daemon-kvm|swtpm)", "^(man-db|mandoc)", "^dbus"],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-exportserverbase-aarch64",
    lock_file = "//rpm/lock-files:exportserverbase-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["tar"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-pr-helper-aarch64",
    lock_file = "//rpm/lock-files:pr-helper-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["qemu-pr-helper"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-sidecar-shim-aarch64",
    lock_file = "//rpm/lock-files:sidecar-shim-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["python3"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

bazeldnf.config(
    name = "bazeldnf-passt-aarch64",
    lock_file = "//rpm/lock-files:passt-aarch64.json",
    nobest = True,
    repofile = "//rpm:repo.yaml",
    rpms = ["passt"],
    rpm_repository_prefix = "bazeldnf-aarch64",
    excludes = [],
    architectures = ["aarch64"],
)

use_repo(
    bazeldnf,
    "bazeldnf-centos-aarch64",
    "bazeldnf-testimage-aarch64",
    "bazeldnf-libvirt-aarch64",
    "bazeldnf-sandboxroot-aarch64",
    "bazeldnf-launcherbase-aarch64",
    "bazeldnf-handlerbase-aarch64",
    "bazeldnf-libguestfstools-aarch64",
    "bazeldnf-exportserverbase-aarch64",
    "bazeldnf-pr-helper-aarch64",
    "bazeldnf-sidecar-shim-aarch64",
    "bazeldnf-passt-aarch64",
)
