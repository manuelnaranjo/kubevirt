load("@io_bazel_rules_go//go:def.bzl", "go_binary", "go_library")
load("@rules_oci//oci:defs.bzl", "oci_image", "oci_load", "oci_push")
load("@rules_pkg//:pkg.bzl", "pkg_tar")

go_library(
    name = "go_default_library",
    srcs = ["main.go"],
    importpath = "kubevirt.io/kubevirt/cmd/sidecars/network-passt-binding",
    visibility = ["//visibility:private"],
    deps = [
        "//cmd/sidecars/network-passt-binding/server:go_default_library",
        "//pkg/hooks:go_default_library",
        "//pkg/hooks/info:go_default_library",
        "//pkg/hooks/v1alpha3:go_default_library",
        "//staging/src/kubevirt.io/client-go/log:go_default_library",
        "//vendor/google.golang.org/grpc:go_default_library",
    ],
)

go_binary(
    name = "network-passt-binding",
    embed = [":go_default_library"],
    visibility = ["//visibility:public"],
)

pkg_tar(
    name = "get-version",
    srcs = ["//:get-version"],
    package_dir = "/",
)

pkg_tar(
    name = "network-passt-binding-tar",
    srcs = [":network-passt-binding"],
    package_dir = "/",
)

oci_image(
    name = "network-passt-binding-image",
    base = "//bazel/compat:go_image_base",
    entrypoint = ["/network-passt-binding"],
    tars = [
        "//:passwd-tar",
        ":network-passt-binding-tar",
        ":get-version",
    ],
    user = "1001",
)

oci_push(
    name = "push",
    image = ":network-passt-binding-image",
    repository = "quay.io/kubevirt/network-passt-binding",
    visibility = ["//visibility:public"],
)

oci_load(
    name = "load",
    image = ":network-passt-binding-image",
    repo_tags = ["bazel/network-passt-binding:latest"],
)
